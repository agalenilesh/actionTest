#include <stdio.h>
#include <stdbool.h>
#include <abcd.h>



void func(char* arg)
{
  char buf11[10];
  char buf2[20];

  strncpy(buf1, arg, sizeof(buf2));
}


int  main() {

    int val = 1;
    char *abcd;
    unsigned int val1 = -1;
    if (val != val1) {
    printf("hello from project 2");
   }

return  0;

}
